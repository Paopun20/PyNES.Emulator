name: Build PyNES Emulator

permissions:
  contents: write
  actions: write
  statuses: write

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch: {}

env:
  PYTHON_VERSION: 3.14.0

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
        python-version: [$PYTHON_VERSION]
      fail-fast: false

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "requirements.txt"

      - name: Setup UPX
        uses: crazy-max/ghaction-upx@v3
        with:
          install-only: true

      - name: Get UPX path
        id: upx
        run: |
          UPX_PATH=$(which upx || echo "")
          echo "upx_path=$UPX_PATH" >> $GITHUB_OUTPUT

      - name: Cache pip packages
        id: pip-cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('app/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-
            ${{ runner.os }}-pip-

      - name: Cache PyInstaller
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pyinstaller
            ~/AppData/Local/pyinstaller
            ~/Library/Caches/pyinstaller
          key: ${{ runner.os }}-pyinstaller-${{ matrix.python-version }}-${{ hashFiles('pynes.spec') }}
          restore-keys: |
            ${{ runner.os }}-pyinstaller-${{ matrix.python-version }}-
            ${{ runner.os }}-pyinstaller-

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            app/**/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('app/**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache build artifacts
        uses: actions/cache@v4
        with:
          path: |
            build/
            *.so
            *.pyd
          key: ${{ runner.os }}-build-${{ matrix.python-version }}-${{ hashFiles('setup.py') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ matrix.python-version }}-
            ${{ runner.os }}-build-

      - name: Install Python dependencies
        run: |
          uv pip install --system --upgrade pip
          uv pip install --system --upgrade -r requirements.txt
          uv pip install --system --upgrade git+https://github.com/pyinstaller/pyinstaller.git

      - name: Build Extensions
        run: python setup.py build_ext --inplace

      - name: Build PyNES
        id: build
        run: |
          echo "build_success=false" >> $GITHUB_OUTPUT
          if [[ -n "${{ steps.upx.outputs.upx_path }}" ]]; then
            uv run pyinstaller pynes.spec --clean --noconfirm --upx-dir "$(dirname "${{ steps.upx.outputs.upx_path }}")"
          else
            uv run pyinstaller pynes.spec --clean --noconfirm
          fi
          echo "build_success=true" >> $GITHUB_OUTPUT

      - name: Verify Build Output
        run: |
          test -f dist/pynes${"${{ matrix.os }}" == "windows-latest" ? ".exe" : ""} || exit 1

      - name: Prepare Upload Directory
        run: |
          mkdir -p upload
          cp cp dist/pynes${"${{ matrix.os }}" == "windows-latest" ? ".exe" : ""} upload/ 
          cp app/icon.ico upload/ 2>/dev/null || true
          cp -r assets/* upload/ 2>/dev/null || true

      - name: Upload Artifact
        if: steps.build.outputs.build_success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: pynes-${{ matrix.os }}-py${{ matrix.python-version }}
          path: upload/
          retention-days: 90
